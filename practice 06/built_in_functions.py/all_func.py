'''
abs()
Возвращает абсолютное значение числа.

all()
Возвращает True, если все элементы итерируемого объекта истинны.

any()
Возвращает True, если хотя бы один элемент итерируемого объекта истинный.

aiter()
Возвращает асинхронный итератор из асинхронного итерируемого объекта.

anext()
Возвращает следующий элемент из асинхронного итератора.

ascii()
Возвращает читаемую версию объекта, заменяя не-ASCII символы escape-последовательностями.

bin()
Возвращает двоичное представление числа.

bool()
Возвращает логическое значение указанного объекта.

breakpoint()
Запускает встроенный отладчик в месте вызова функции.

bytearray()
Возвращает изменяемый массив байтов.

bytes()
Возвращает объект байтов.

callable()
Возвращает True, если объект можно вызвать как функцию.

chr()
Возвращает символ по указанному Unicode-коду.

classmethod()
Преобразует метод в метод класса.

compile()
Компилирует исходный код и возвращает объект для выполнения.

complex()
Возвращает комплексное число.

delattr()
Удаляет указанный атрибут объекта.

dict()
Возвращает словарь.

dir()
Возвращает список свойств и методов объекта.

divmod()
Возвращает частное и остаток от деления.

enumerate()
Возвращает объект enumerate для нумерации элементов.

eval()
Вычисляет и выполняет выражение.

exec()
Выполняет указанный код.

filter()
Фильтрует элементы итерируемого объекта.

float()
Возвращает число с плавающей точкой.

format()
Форматирует указанное значение.

frozenset()
Возвращает неизменяемое множество.

getattr()
Возвращает значение указанного атрибута.

globals()
Возвращает глобальную таблицу символов.

hasattr()
Проверяет наличие атрибута у объекта.

hash()
Возвращает хэш-значение объекта.

help()
Запускает встроенную систему помощи.

hex()
Преобразует число в шестнадцатеричное.

id()
Возвращает уникальный идентификатор объекта.

input()
Позволяет пользователю вводить данные.

int()
Возвращает целое число.

isinstance()
Проверяет, является ли объект экземпляром класса.

issubclass()
Проверяет, является ли класс подклассом другого класса.

iter()
Возвращает объект-итератор.

len()
Возвращает длину объекта.

list()
Возвращает список.

locals()
Возвращает локальную таблицу символов.

map()
Применяет функцию к каждому элементу итерируемого объекта.

max()
Возвращает наибольший элемент.

memoryview()
Возвращает объект представления памяти.

min()
Возвращает наименьший элемент.

next()
Возвращает следующий элемент итератора.

object()
Создаёт новый объект.

oct()
Преобразует число в восьмеричное.

open()
Открывает файл и возвращает файловый объект.

ord()
Возвращает Unicode-код символа.

pow()
Возвращает число x в степени y.

print()
Выводит данные на экран.

property()
Используется для управления свойствами объекта.

range()
Возвращает последовательность чисел.

repr()
Возвращает строковое представление объекта.

reversed()
Возвращает обратный итератор.

round()
Округляет число.

set()
Возвращает множество.

setattr()
Устанавливает атрибут объекта.

slice()
Возвращает объект среза.

sorted()
Возвращает отсортированный список.

staticmethod()
Преобразует метод в статический.

str()
Возвращает строку.

sum()
Возвращает сумму элементов.

super()
Возвращает объект родительского класса.

tuple()
Возвращает кортеж.

type()
Возвращает тип объекта.

vars()
Возвращает словарь атрибутов объекта.

zip()
Объединяет элементы нескольких итерируемых объектов.

import()
Импортирует модуль динамически и возвращает объект модуля.
'''